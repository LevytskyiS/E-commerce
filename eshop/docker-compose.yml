version: '3'

services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: ${PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - ./db:/var/lib/postgresql/data
  redis:
    image: redis
    ports:
      - "6379:6379"
